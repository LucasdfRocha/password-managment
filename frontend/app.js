// Minimal bootstrap after modularization
// This file contains a very small initialization sequence. The bulk of the application
// logic has been moved into modular files under `frontend/js/` and those scripts must
// be loaded before this file in `index.html`.

// Set initial status and empty state
setStatus("Aguardando login.");
updateEmptyState();


